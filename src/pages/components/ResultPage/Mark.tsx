import React from "react";

type Props = {
  isCorrect: boolean[];
};

const Mark = (props: Props) => {
  const { isCorrect } = props;
  
  return (
    <>
      <div></div>
    </>
  );
};
export default Mark;
